name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  feature-surface-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run feature surface audit
        run: python scripts/audit_feature_surfaces.py

  backend-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install test dependencies
        run: pip install -r backend/requirements-dev.txt
      - name: Pytest
        run: python -m pytest backend/tests -q

  desktop-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - name: Build desktop UI
        working-directory: apps/desktop
        run: |
          npm ci
          npm test
          npm run build

  hardware-policy-fixtures:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install backend test dependencies
        run: pip install -r backend/requirements-dev.txt
      - name: Validate every hardware policy fixture
        working-directory: backend
        run: |
          python -m dreamforge_hardware_benchmark
          python -m pytest tests/test_gpu_detect.py tests/test_comfy_launch.py tests/test_hardware_benchmark.py -q
